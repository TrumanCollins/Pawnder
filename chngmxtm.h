void change_max_time(
        int player,            // The color of the player to change.
        int secondary = False  // True if change secondary time control.
    );
    // This function will allow the user to change the time control for
    // the computer.
